version: "3.9"
services:
  platform:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - ./storage:/app/storage
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - RELOAD=false
    restart: unless-stopped
